{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "The Spotifie window. Core window and app events only: no filesystem, shell, process, dialog, HTTP or updater permission. Applies to the bundled application only; no remote page is granted anything.",
    "windows": ["main"],
    "permissions": ["core:default"]
}
